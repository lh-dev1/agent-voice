# 医疗桌面语音助手（唤醒\+ASR\+桌面挂件\+业务透传）技术方案

# 1\. 方案概述

## 1\.1 项目背景

为医疗工作站提供轻量化、离线化语音辅助工具，实现**语音唤醒、口语指令识别、桌面悬浮可视化、结构化指令推送业务端**能力。系统要求常驻后台、极低资源占用、不开源收费、不上传云端数据，完全本地离线运行。

## 1\.2 核心需求

- **语音唤醒**：自定义唤醒词（小图小图），后台持续监听，低误唤醒。

- **语音指令识别**：支持医疗短句指令，本地ASR识别，无需联网。

- **桌面挂件**：置顶悬浮小球、可拖拽、状态可视化（待机/聆听/识别/完成）。

- **业务透传**：只做语音采集\+解析，结构化JSON推送业务端，不处理业务逻辑。

- **轻量化**：整机低CPU、低内存，适配普通办公/医疗工控机。

- **开源免费**：全部组件开源、无商业授权费用、可二次开发。

## 1\.3 设计原则

- **极简架构**：不使用大模型，拒绝重型AI框架。

- **休眠机制**：仅唤醒词常驻，ASR识别后自动释放模型资源。

- **解耦设计**：语音层与业务层完全隔离，只做数据透传。

- **可视化状态**：桌面挂件直观展示当前工作状态。

# 2\. 总体架构设计

## 2\.1 架构分层

整体分为四层，单向数据流，模块完全解耦：

1. **音频采集层**：麦克风实时收音、音频预处理。

2. **语音引擎层**：离线唤醒 \+ 轻量ASR指令识别。

3. **可视化层**：桌面悬浮挂件、状态渲染、拖拽交互。

4. **业务通信层**：指令结构化、HTTP推送业务端。

## 2\.2 运行流程

待机监听 → 唤醒词触发 → 自动收音 → ASR转文字 → 指令结构化解析 → JSON推送业务端 → 恢复待机休眠。

## 2\.3 整体拓扑图（文字版）

麦克风 → 唤醒引擎\(常驻\) → 触发唤醒 → 开启收音 → ASR模型\(临时加载\) → 指令解析 → HTTP接口 → 业务系统

# 3\. 技术选型（全部开源免费）

|模块|选型|作用|资源占用|开源协议|
|---|---|---|---|---|
|语音唤醒|Porcupine|离线自定义唤醒词、极低功耗常驻|内存20MB、CPU 0\~1%|Apache2\.0|
|语音识别ASR|SenseVoice\-Small|中文短句识别、医疗指令专用、轻量化|模型45MB、唤醒瞬时加载|Apache2\.0|
|桌面挂件GUI|PyQt6|置顶悬浮、可拖拽、透明小球|内存10MB以内|GPL|
|音频采集|PyAudio|低延迟麦克风采样|极低占用|MIT|
|通信传输|HTTP/IPC|结构化指令推送业务端|无占用|—|

# 4\. 核心模块详细设计

## 4\.1 语音唤醒模块

- **唤醒词**：自定义「小图小图」

- **采样率**：16000Hz

- **灵敏度**：0\.6（防误唤醒）

- **运行方式**：后台子线程常驻，不阻塞UI

- **特性**：断句识别、抗环境噪音、医疗室内适配

## 4\.2 轻量ASR识别模块

- **模型**：SenseVoice\-Small（中文最优轻量模型）

- **触发机制**：唤醒成功后才加载模型，识别结束自动释放显存/内存

- **识别场景**：短指令（1\~5秒），适配医疗口语

- **能力**：支持轻微口音、低噪声环境、极快推理速度

## 4\.3 桌面悬浮挂件模块

- **样式**：圆形悬浮小球、40px、透明背景、置顶穿透窗口

- **拖拽**：左键自由拖拽，常驻桌面最上层

- **状态码定义**：
        

    1. 灰色：待机休眠（仅唤醒引擎运行）

    2. 绿色：唤醒成功，聆听指令

    3. 黄色：ASR识别中

    4. 蓝色：指令推送业务端完成

## 4\.4 指令解析模块（无大模型、纯规则）

采用正则\+关键词匹配，毫秒级解析，无AI推理开销，适配医疗固定指令集。

## 4\.5 业务透传模块

- **传输方式**：HTTP POST JSON

- **设计思想**：语音层只做采集解析，**不参与任何业务逻辑**

- **可扩展**：支持改为WebSocket、本地IPC、Socket进程通信

# 5\. 医疗指令协议（推送业务端JSON格式）

|口语指令|intent意图|params参数|
|---|---|---|
|小图小图|wake\_up|\{\}|
|调取患者123456|fetch\_patient|\{\&\#34;patient\_id\&\#34;:\&\#34;123456\&\#34;,\&\#34;patient\_name\&\#34;:\&\#34;\&\#34;\}|
|调取患者张三|fetch\_patient|\{\&\#34;patient\_id\&\#34;:\&\#34;\&\#34;,\&\#34;patient\_name\&\#34;:\&\#34;张三\&\#34;\}|
|呼叫下一个患者|call\_next\_patient|\{\}|
|打开语音功能|open\_voice|\{\}|
|开始录音|start\_record|\{\}|
|暂停录音|pause\_record|\{\}|
|关闭录音|stop\_record|\{\}|

**通用JSON结构**

```json
{
    "intent": "指令意图",
    "params": {},
    "feedback": "助手提示语"
}
```

# 6\. 硬件资源占用说明（重点）

## 6\.1 待机状态（99%运行时间）

- CPU占用：0%\~1%

- 内存占用：50\~70MB

- 运行模块：唤醒引擎 \+ 桌面挂件

- ASR模型：完全卸载，不占用资源

## 6\.2 识别状态（瞬时几秒）

- CPU占用：瞬时峰值15%\~25%

- 内存占用：峰值120MB左右

- 结束后：立刻释放ASR内存，回归待机低占用

## 6\.3 硬件最低配置要求

- CPU：双核及以上

- 内存：4GB

- 系统：Windows10/Windows11

- 无需独立显卡、无需CUDA

# 7\. 部署方案

## 7\.1 依赖环境

```bash
pip install pvporcupine pyaudio pyqt6 sensevoice requests
```

## 7\.2 部署文件清单

- main\.py：主程序源码

- xiaotuxiaotu\.ppn：自定义唤醒词模型文件

- config\.json：业务接口配置文件

## 7\.3 打包发布

支持Pyinstaller打包为单exe，无需安装Python，可医疗电脑直接双击运行，支持开机自启。

# 8\. 扩展预留（后期可加）

- 离线轻量TTS语音播报

- 指令推荐候选列表（挂件弹窗提示）

- 降噪算法优化医疗嘈杂环境

- 日志本地保存、指令统计

- 切换WebSocket通信方式

# 9\. 方案优势总结

1. **纯本地离线**：不上云、无数据泄露，符合医疗保密要求。

2. **极致轻量化**：拒绝大模型，常驻极低占用，不卡顿医疗工作站。

3. **业务解耦**：只透传结构化指令，不改业务代码、不侵入业务系统。

4. **全部开源免费**：无授权费、无服务费、可永久商用。

5. **可视化挂件**：直观状态反馈，医护人员上手简单。

> （注：文档部分内容可能由 AI 生成）
